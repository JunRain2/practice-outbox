spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

outbox:
  scheduler:
    interval-seconds: 60
    recovery-timeout-seconds: 30
  retry:
    default:
      max-count: 5
      base-seconds: 60
    strategies:
      MOCK_CREATED:
        max-count: 5
        base-seconds: 60
